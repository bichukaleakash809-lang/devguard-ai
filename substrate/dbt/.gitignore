# dbt build output. Regenerated by `dbt run` + `dbt docs generate`, which
# recipes/dbt.yml requires you to run before ingesting anyway — committing these
# would ship stale lineage describing a world that no longer exists, which after
# the hero-loop rename is exactly the wrong answer.
target/
logs/
dbt_packages/
.user.yml
